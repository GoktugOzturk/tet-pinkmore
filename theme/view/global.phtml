<!DOCTYPE html>
<html lang="tr">
<?= view::load("head") ?>
<body lang="tr">
<?= view::facebook_sdk() ?>
<?= view::load("header") ?>
<section>
    <?= view::showMessages('class="messageBox container" style="display:none"') ?>
    <div class="container">
        <? if (view::has_blocks()) { ?>
            <div class="row">
                <? view::show_contents("col-sm-9 col-xs-12 pull-right") ?>
                <? view::show_blocks("col-sm-3 col-xs-12 pull-left") ?>
            </div>
        <? } else { ?>
            <? view::show_contents() ?>
        <? } ?>
    </div>
</section>
<!-- container sonu -->
<?= view::load("footer") ?>
</body>
</html>
